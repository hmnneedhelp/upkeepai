import Navbar from "@/components/Navbar";
import Profile from "./Profile";
import initMap from '@/components/Gmaps'
export default function MainPage() {
    return(
        <main className="absolute w-screen h-screen">
            <Navbar></Navbar>
            
        </main>
    )
}
